# BASE-1: Canonical Definitions

**Version:** v1.0-LOCKED  
**Status:** LOCKED — นิยามศูนย์กลางต้องล็อกก่อนเขียนอะไรต่อ

---

## Purpose

สิ่งแรกสุดที่ต้องล็อก ไม่งั้นทุกอย่างจะ drift

**ต้องมีไฟล์/เอกสารนี้ก่อนเขียนอะไรต่อ**

ถ้า BASE-1 ไม่แน่น:
- → Debug ไม่ได้
- → Audit ไม่ผ่าน
- → คนอื่นต่อไม่ได้

---

## Core Principles

### 📌 ห้าม:
- ใช้คำเดียวกันหลายความหมาย
- เปลี่ยนนิยามตาม module
- ใช้คำที่คลุมเครือหรือตีความได้หลายแบบ

### ✅ ต้อง:
- นิยามให้ตายตัว (immutable)
- ใช้คำเดียวกันตลอดทั้งระบบ
- อ้างอิงกลับมาที่เอกสารนี้เสมอ

---

## Canonical Definitions

### 1. Trajectory

**Definition:**
Trajectory คือ **physical object** ที่เป็นลำดับของ states ที่เชื่อมต่อกัน

**Properties:**
- Trajectory = [S₀, S₁, S₂, ...]
- แต่ละ state มี timestamp
- Trajectory มี trace_id (unique identifier)
- Trajectory เป็น immutable sequence (append-only)

**NOT:**
- ❌ ไม่ใช่ path หรือ route
- ❌ ไม่ใช่ sequence ของ events
- ❌ ไม่ใช่ history หรือ log

**Usage:**
- ใช้ในการติดตาม evolution ของระบบ
- ใช้ในการ recall memory
- ใช้ในการทำ resonance matching

---

### 2. Energy

**Definition:**
Energy คือ **dimensionless scalar** ที่วัดระดับ "activation" หรือ "potential" ของระบบ

**Properties:**
- Energy ∈ ℝ (real number)
- Energy ≥ 0 (สำหรับพลังงานส่วนใหญ่)
- Energy ไม่มีหน่วย (dimensionless)
- Energy เป็น abstract quantity (ไม่ใช่พลังงานจริง)

**Types:**
- **Perception Energy (ΔEΨ)**: พลังงานการรับรู้
- **Reflex Energy (E_reflex)**: พลังงานปฏิกิริยาทันที
- **Cognitive Energy (E_mind)**: พลังงานการคิด
- **Binding Energy (E_bind)**: พลังงานการเชื่อมโยง
- **Memory Energy (E_mem)**: พลังงานการบันทึกความจำ

**NOT:**
- ❌ ไม่ใช่พลังงานจริง (physical energy)
- ❌ ไม่ใช่ความน่าจะเป็น (probability)
- ❌ ไม่ใช่คะแนนหรือ rating

**Usage:**
- ใช้ในการตัดสินใจว่าจะประมวลผลหรือไม่
- ใช้ในการกำหนดลำดับความสำคัญ
- ใช้ในการประเมินสถานะของระบบ

---

### 3. Memory

**Definition:**
Memory คือ **passive field** ที่เก็บข้อมูลในรูปแบบ attractor states

**Properties:**
- Memory เป็น passive (ไม่คิดเอง)
- Memory เก็บเป็น field/pattern
- Memory มี strength (ความแรง)
- Memory มี decay (การเสื่อมตามเวลา)

**Types:**
- **Episodic Memory**: เก็บเหตุการณ์เฉพาะ
- **Semantic Memory**: เก็บความหมาย/หลักการ
- **Procedural Memory**: เก็บวิธีทำ (action patterns)
- **Working Memory**: เก็บชั่วคราว (7 ± 2 items)

**NOT:**
- ❌ ไม่ใช่ database หรือ key-value store
- ❌ ไม่ใช่ cache หรือ buffer
- ❌ ไม่ใช่ active reasoning system

**Usage:**
- ใช้ในการ recall patterns
- ใช้ในการทำ resonance matching
- ใช้ในการ consolidate knowledge

---

### 4. Gate

**Definition:**
Gate คือ **deterministic judge** ที่ตัดสินว่าจะอนุญาต/ตรวจสอบ/บล็อก

**Properties:**
- Gate เป็น deterministic (same input → same output)
- Gate ไม่เรียนรู้ (static rules)
- Gate ตรวจสอบได้ (auditable)
- Gate ย้อนรอยได้ (traceable)

**Types:**
- **Admission Gate**: ตัดสินว่าจะสร้าง trajectory หรือไม่
- **Decision Gate**: ตัดสินว่าจะดำเนินการต่อหรือไม่
- **Entropy Gate**: ตัดสินว่าจะบล็อก noise หรือไม่

**Output:**
- `ALLOW`: อนุญาตให้ดำเนินการต่อ
- `REVIEW`: ต้องตรวจสอบเพิ่มเติม
- `BLOCK`: ป้องกันไม่ให้ดำเนินการ

**NOT:**
- ❌ ไม่ใช่ classifier หรือ ML model
- ❌ ไม่ใช่ heuristic หรือ rule-of-thumb
- ❌ ไม่ใช่ adaptive system

**Usage:**
- ใช้ในการควบคุมความปลอดภัย
- ใช้ในการป้องกัน noise/hallucination
- ใช้ในการตรวจสอบ constraints

---

### 5. Core vs Interface

**Definition:**

**Core:**
- สูตรแกนกลาง (CORE-1 ถึง CORE-9)
- LOCKED — ห้ามแก้ไขโดยไม่มีการ review
- Deterministic และ reproducible
- เป็น foundation ของระบบ

**Interface:**
- การเชื่อมต่อระหว่าง modules
- สามารถเปลี่ยน implementation ได้
- แต่ต้องรักษา contract
- เป็น abstraction layer

**Properties:**

| Aspect | Core | Interface |
|--------|------|-----------|
| **Mutability** | Immutable (LOCKED) | Mutable (with contract) |
| **Review Required** | Yes (formal) | Yes (technical) |
| **Dependencies** | None (foundation) | Depends on Core |
| **Examples** | CORE-1: ΔEΨ | API, FFI, Adapters |

**NOT:**
- ❌ Core ≠ Implementation detail
- ❌ Interface ≠ Core formula
- ❌ Core ≠ Configuration

**Usage:**
- Core: ใช้ในการคำนวณพลังงาน
- Interface: ใช้ในการเชื่อมต่อกับ external systems

---

## Cross-References

### Trajectory ↔ Energy
- Trajectory ใช้ Energy ในการตัดสินใจว่าจะสร้างหรือไม่
- Energy ถูกคำนวณจาก states ใน Trajectory

### Energy ↔ Memory
- Energy ใช้ในการตัดสินใจว่าจะบันทึก Memory หรือไม่
- Memory strength ขึ้นกับ Energy

### Memory ↔ Gate
- Gate ใช้ Memory ในการทำ resonance matching
- Memory recall ต้องผ่าน Gate

### Gate ↔ Core
- Gate ใช้ Core formulas ในการตัดสินใจ
- Core formulas เป็น foundation ของ Gate logic

---

## Version History

- **v1.0-LOCKED**: Initial canonical definitions

---

## Notes

- **Lock Status**: LOCKED — ห้ามแก้ไขโดยไม่มีการ review
- **Review Process**: ต้องผ่าน formal review ก่อนแก้ไข
- **Impact**: การแก้ไขจะกระทบทั้งระบบ

