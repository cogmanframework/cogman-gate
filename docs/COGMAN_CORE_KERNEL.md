# 🔒 Cogman Core Kernel (CCK)

**Version:** v2.0–LOCKED  
**Status:** LOCKED — สูตรแกนกลาง 9 สูตรนี้เป็น core ของระบบ ห้ามแก้ไขโดยไม่มีการ review

---

## CCK Summary

| ID | Formula | Description |
|----|---------|-------------|
| **CORE-1** | ΔEΨ | Energy of Perception |
| **CORE-2** | E_reflex | Reflex Energy |
| **CORE-3** | ΔEΨ_theta | Directional Reflex Energy |
| **CORE-4** | E_mind | Cognitive Energy |
| **CORE-5** | E_coherence | Coherence Energy |
| **CORE-6** | E_neural | Neuro-Energetic Sum (interface/placeholder) |
| **CORE-7** | E_bind | Binding Energy |
| **CORE-8** | E_mem | Memory Encoding Energy |
| **CORE-9** | G_decision | Decision Gate Verdict |

---

## CORE-1) ΔEΨ — Energy of Perception

**Name:** ΔEΨ (Energy of Perception)

**Description:**
  - ดัชนีพลังงานของการรับรู้
  - แทนระดับพลังงานที่ระบบจัดสรรให้กับการ "รับและจับสัญญาณ"
  - ใช้ก่อนการแตกคำ แตกวลี หรือแตกโครงสร้าง
  - ไม่ใช่ความหมาย ไม่ใช่อารมณ์ ไม่ใช่การตัดสินใจ

**Formula:**
```
ΔEΨ = I × |P| × S_a × (1 − H)
```
หรือ
```
ΔEΨ = I × P × S × (1 − H)
```

**Variables:**
  - `I`: ความเข้มของสัญญาณขาเข้า (Intensity) [I ≥ 0]
  - `P`: Polarity หรือความแรงของรูปแบบที่ตรวจพบ [P ∈ ℝ] (ถ้าใช้ |P| จะใช้ค่าสัมบูรณ์)
  - `S_a` หรือ `S`: Stability หรือความไวของการรับรู้ [0 ≤ S ≤ 1]
  - `H`: สัญญาณรบกวน / ความไม่แน่นอน (Entropy) [0 ≤ H ≤ 1]

**Function:**
  - `energy_to_perceive_signal(I, P, S_a, H)`

**Usage:**
  - ใช้ในการประเมินพลังงานที่ต้องใช้สำหรับการรับรู้สัญญาณขาเข้า
  - เป็น input เริ่มต้นสำหรับกระบวนการ perception layer
  - ใช้กำหนดลำดับความสำคัญในการประมวลผลสัญญาณ

**Constraints:**
  - ต้องไม่ใช้ตีความเชิงแพทย์หรือชีวภาพ
  - ไม่ใช่การวัดความหมายหรืออารมณ์
  - ใช้เฉพาะในบริบท engineering simulation เท่านั้น
  - H ต้องอยู่ในช่วง [0, 1] เสมอ

---

## CORE-2) E_reflex — Reflex Energy

**Name:** E_reflex (Reflex Energy)

**Description:**
  - พลังงานปฏิกิริยาทันทีหลังการรับรู้
  - ยังไม่ผ่านการคิดหรือ reasoning
  - ใช้กำหนดว่าจะ "ตอบสนองเร็ว" หรือ "รอประมวลผล"

**Formula:**
```
E_reflex = ΔEΨ × (1 + A)
```

**Variables:**
  - `ΔEΨ`: พลังงานการรับรู้ (จาก CORE-1) [ΔEΨ ≥ 0]
  - `A`: ระดับการรับรู้ตัวเองของระบบ (awareness) [0 ≤ A ≤ 1]

**Function:**
  - `reflex_energy(delta_E_psi, A)`

**Usage:**
  - ใช้ในการตัดสินใจว่าจะตอบสนองทันทีหรือรอการประมวลผลเพิ่มเติม
  - เป็นส่วนหนึ่งของ reflex layer
  - ใช้ร่วมกับ Decision Gate เพื่อควบคุมการตอบสนอง

**Constraints:**
  - ต้องไม่ใช้แทนการตัดสินใจขั้นสุดท้าย
  - ไม่ใช่พลังงานสำหรับ reasoning หรือ deep processing
  - ใช้เฉพาะสำหรับการประเมินปฏิกิริยาทันทีเท่านั้น

---

## CORE-3) ΔEΨ_theta — Directional Reflex Energy

**Name:** ΔEΨ_theta (Directional Reflex Energy)

**Description:**
  - พลังงาน reflex ที่มีทิศทาง
  - ใช้ควบคุมการหันของ reasoning

**Formula:**
```
ΔEΨ_theta = ΔEΨ × theta_phase × 0.2
```

**Variables:**
  - `ΔEΨ`: พลังงานการรับรู้ (Energy of Perception) [≥ 0]
  - `theta_phase`: phase angle สำหรับทิศทาง [0 ≤ theta_phase < 2π]
  - `0.2`: scaling factor (constant)

**Function:**
  - `directional_reflex_energy(delta_E_psi, theta_phase)`

**Usage:**
  - ใช้ในการควบคุมทิศทางของ reasoning
  - ใช้ในการกำหนดการหันของกระบวนการคิด
  - ใช้ใน reflex layer สำหรับการควบคุมทิศทาง

**Constraints:**
  - ต้องไม่ใช้แทนการตัดสินใจทิศทางโดยตรง
  - ไม่ใช่พลังงานสำหรับ reasoning ขั้นลึก
  - ใช้เฉพาะสำหรับการควบคุมทิศทางของ reflex เท่านั้น
  - theta_phase ต้อง normalize ในช่วง [0, 2π)

---

## CORE-4) E_mind — Cognitive Energy

**Name:** E_mind (Cognitive Energy)

**Description:**
  - พลังงานสำหรับการสร้างความหมายและโครงสร้างเชิงเหตุผล
  - เกิดหลังการรับรู้ ไม่ใช่ reflex

**Formula:**
```
E_mind = (I + A + (1 − H)) / 3
```

**Variables:**
  - `I`: ความเข้มของสัญญาณขาเข้า [I ≥ 0]
  - `A`: ระดับการรับรู้ตัวเองของระบบ (awareness) [0 ≤ A ≤ 1]
  - `H`: สัญญาณรบกวน / ความไม่แน่นอน [0 ≤ H ≤ 1]

**Function:**
  - `cognitive_energy(I, A, H)`

**Usage:**
  - ใช้ในการประเมินพลังงานที่พร้อมสำหรับการสร้างความหมาย
  - เป็น input สำหรับ reasoning layer
  - ใช้ในการตัดสินใจว่าจะใช้พลังงานเท่าไหร่ในการประมวลผลเชิงลึก

**Constraints:**
  - ต้องไม่ใช้แทน E_reflex (ใช้สำหรับ reflex)
  - ไม่ใช่พลังงานสำหรับ memory encoding โดยตรง
  - ใช้เฉพาะสำหรับ cognitive processing เท่านั้น

---

## CORE-5) E_coherence — Coherence Energy

**Name:** E_coherence (Coherence Energy)

**Description:**
  - ระดับความสอดคล้องภายในของระบบ
  - ใช้ดูว่าความคิด "แตก" หรือ "รวมตัวดี"

**Formula:**
```
E_coherence = (S + A + (1 − H)) / 3
```

**Variables:**
  - `S`: ระดับความเสถียรของระบบ [0 ≤ S ≤ 1]
  - `A`: ระดับการรับรู้ตัวเองของระบบ (awareness) [0 ≤ A ≤ 1]
  - `H`: สัญญาณรบกวน / ความไม่แน่นอน [0 ≤ H ≤ 1]

**Function:**
  - `coherence_energy(S, A, H)`

**Usage:**
  - ใช้ในการประเมินความสอดคล้องภายในของระบบ
  - เป็นส่วนประกอบของ E_bind (Binding Energy)
  - ใช้ในการตรวจสอบความเสถียรของกระบวนการคิด

**Constraints:**
  - ต้องไม่ใช้แทนการวัดความถูกต้องของความคิด
  - ไม่ใช่การประเมินคุณภาพของ output
  - ใช้เฉพาะสำหรับการประเมินความสอดคล้องภายในเท่านั้น

---

## CORE-6) E_neural — Neuro-Energetic Sum

**Name:** E_neural (Neuro-Energetic Energy)

**Description:**
  - ผลรวมพลังงานเชิงระบบประสาท (เชิงสัญลักษณ์)
  - ไม่ใช่ค่าชีวภาพจริง
  - **Note:** E_neural เป็น interface/placeholder ที่สามารถนิยามได้หลายแบบ (simple sum, weighted sum, etc.)

**Formula:**
```
E_neural = dopamine + serotonin + oxytocin + adrenaline + cortisol
```

**Variables:**
  - `dopamine`: ดัชนีโดปามีน (เชิงสัญลักษณ์) [≥ 0]
  - `serotonin`: ดัชนีเซโรโทนิน (เชิงสัญลักษณ์) [≥ 0]
  - `oxytocin`: ดัชนีออกซิโทซิน (เชิงสัญลักษณ์) [≥ 0]
  - `adrenaline`: ดัชนีอะดรีนาลีน (เชิงสัญลักษณ์) [≥ 0]
  - `cortisol`: ดัชนีคอร์ติซอล (เชิงสัญลักษณ์) [≥ 0]

**Function:**
  - `neural_energetic_sum(dopamine, serotonin, oxytocin, adrenaline, cortisol)`

**Usage:**
  - ใช้ในการคำนวณพลังงานรวมของระบบประสาทเชิงสัญลักษณ์
  - เป็นส่วนประกอบของ E_bind (Binding Energy)
  - ใช้ในการประเมินสถานะพลังงานโดยรวมของระบบ

**Constraints:**
  - ต้องไม่ใช้ตีความเชิงแพทย์หรือชีวภาพ
  - ค่าทั้งหมดเป็นสัญลักษณ์เชิงวิศวกรรมเท่านั้น
  - ไม่ใช่การวัดค่าชีวภาพจริง
  - ใช้เฉพาะในบริบท simulation
  - **Interface/Placeholder:** สามารถ implement เป็น simple sum, weighted sum, หรือรูปแบบอื่นได้

---

## CORE-7) E_bind — Binding Energy

**Name:** E_bind (Binding Energy)

**Description:**
  - พลังงานการเชื่อมโยงระหว่างการรับรู้ + ความคิด + สถานะระบบ
  - ใช้ก่อนการบันทึกความจำ

**Formula:**
```
E_bind = E_mind × E_neural × E_coherence
```

**Variables:**
  - `E_mind`: พลังงานการคิด (Cognitive Energy) [≥ 0]
  - `E_neural`: พลังงานระบบประสาท (Neuro-Energetic Sum) [≥ 0]
  - `E_coherence`: พลังงานความสอดคล้อง (Coherence Energy) [≥ 0]

**Function:**
  - `binding_energy(E_mind, E_neural, E_coherence)`

**Usage:**
  - ใช้ในการประเมินพลังงานที่พร้อมสำหรับการเชื่อมโยงข้อมูล
  - เป็นส่วนประกอบของ E_mem (Memory Encoding Energy)
  - ใช้ก่อนการตัดสินใจบันทึกความจำ

**Constraints:**
  - ต้องไม่ใช้แทนการตัดสินใจบันทึกความจำโดยตรง
  - ไม่ใช่พลังงานสำหรับการเรียกใช้ความจำ
  - ใช้เฉพาะสำหรับการประเมินพลังงานการเชื่อมโยงเท่านั้น

---

## CORE-8) E_mem — Memory Encoding Energy

**Name:** E_mem (Memory Encoding Energy)

**Description:**
  - พลังงานที่ใช้ตัดสินว่าควรบันทึกความจำหรือไม่

**Formula:**
```
E_mem = A × E_bind × E_pred
```

**Variables:**
  - `A`: ระดับการรับรู้ตัวเองของระบบ (awareness) [0 ≤ A ≤ 1]
  - `E_bind`: พลังงานการเชื่อมโยง (Binding Energy) [≥ 0]
  - `E_pred`: พลังงานการคาดการณ์ (Predictive Energy) [≥ 0]

**Function:**
  - `memory_encoding_energy(A, E_bind, E_pred)`

**Usage:**
  - ใช้ในการตัดสินใจว่าควรบันทึกความจำหรือไม่
  - ใช้ในการประเมินความสำคัญของข้อมูลที่จะบันทึก
  - ใช้ใน memory layer

**Constraints:**
  - ต้องไม่ใช้แทนการตัดสินใจบันทึกความจำโดยตรง
  - ไม่ใช่การวัดคุณภาพของความจำ
  - ใช้เฉพาะสำหรับการประเมินพลังงานการบันทึกเท่านั้น
  - ต้องมี threshold สำหรับการตัดสินใจบันทึก

---

## CORE-9) G_decision — Decision Gate Verdict

**Name:** G_decision (Decision Gate Verdict)

**Description:**
  - ผลลัพธ์การตัดสินใจเชิงวิศวกรรม
  - ไม่มีการเรียนรู้สด
  - ตรวจสอบได้ ย้อนรอยได้

**Formula:**
```
Logic:
  IF rule_fail → BLOCK
  IF Eμ ∈ restrict → BLOCK
  IF H high OR D_traj high → REVIEW
  ELSE → ALLOW
```

**Variables:**
  - `rule_fail`: การละเมิดกฎ (boolean)
  - `Eμ`: Internal Energy Metric [Eμ ∈ ℝ]
  - `restrict`: ช่วงค่าที่ถูกจำกัด (set หรือ range)
  - `H`: สัญญาณรบกวน / ความไม่แน่นอน [0 ≤ H ≤ 1]
  - `D_traj`: ระยะห่าง trajectory (Trajectory Distance) [≥ 0]
  - `high`: threshold สำหรับ H และ D_traj (ต้องกำหนดค่า)

**Function:**
  - `decision_gate(rule_fail, E_mu, restrict, H, D_traj, H_threshold, D_traj_threshold)`

**Output:**
  - `ALLOW`: อนุญาตให้ดำเนินการต่อ
  - `REVIEW`: ต้องตรวจสอบเพิ่มเติม
  - `BLOCK`: ป้องกันไม่ให้ดำเนินการ

**Usage:**
  - ใช้เป็น gate สุดท้ายก่อนการตัดสินใจหรือการดำเนินการ
  - ใช้ในการควบคุมความปลอดภัยของระบบ
  - ใช้ในการตรวจสอบความพร้อมก่อนการดำเนินการ

**Constraints:**
  - ต้องไม่ใช้ตีความเชิงแพทย์หรือจริยธรรม
  - ไม่มีการเรียนรู้สด (static rules)
  - ต้องตรวจสอบได้และย้อนรอยได้เสมอ
  - ใช้เฉพาะในบริบท engineering simulation
  - threshold สำหรับ H และ D_traj ต้องกำหนดไว้ล่วงหน้า
  - restrict range ต้องกำหนดไว้ล่วงหน้า

---

## CCK Formula Dependencies

### Core Flow:
```
Signal Input (I, P, S_a, H)
  ↓
CORE-1: ΔEΨ (Energy of Perception)
  ↓
├─→ CORE-2: E_reflex (Reflex Energy)
├─→ CORE-3: ΔEΨ_theta (Directional Reflex Energy)
└─→ CORE-4: E_mind (Cognitive Energy)

CORE-4: E_mind + CORE-6: E_neural + CORE-5: E_coherence
  ↓
CORE-7: E_bind (Binding Energy)
  ↓
CORE-8: E_mem = A × E_bind × E_pred

All Core Energies
  ↓
CORE-9: G_decision (Decision Gate)
  ↓
ALLOW / REVIEW / BLOCK
```

---

## Notes

- **Version Lock:** v2.0–LOCKED — สูตรทั้ง 9 ตัวนี้ถูก lock แล้ว ห้ามแก้ไขโดยไม่มีการ review
- **Cogman Core Kernel (CCK):** สูตรทั้ง 9 ตัวนี้เป็นแกนกลางของระบบ Cogman Energetic Engine
- **Engineering Only:** สูตรทั้งหมดเป็น engineering simulation เท่านั้น ไม่มีการตีความเชิงแพทย์หรือจริยธรรม
- **Verification:** ทุกสูตรต้องตรวจสอบได้และย้อนรอยได้
- **No Live Learning:** ระบบไม่มีการเรียนรู้สด (static rules)
- **Interface/Placeholder:** CORE-6 (E_neural) สามารถ implement ได้หลายแบบตามความต้องการ

